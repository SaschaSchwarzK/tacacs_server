# Environment configuration for docker-compose

# Web/API token (protects /api/*)
API_TOKEN=test-token

# Admin web UI credentials
# Preferred in development: set a plain password via a local, uncommitted .env file.
# For production, DO NOT store secrets in files — use a secret manager or
# only provide ADMIN_PASSWORD via your deployment environment (not checked in).
ADMIN_USERNAME=admin
# ADMIN_PASSWORD=

# Alternatively, provide a bcrypt hash (exactly 60 chars starting with $2b$).
# If you use a hash, prefer passing it via a secure secret manager or a
# local .env that is NOT committed. Compose will read it verbatim from env_file.
# ADMIN_PASSWORD_HASH=$2b$12$vj2m47XxypTDfG/ZOaUeP.a2lROwySqp7kWzb7OmV/UNHtcOFnA2G

# Optional: HMAC key for auth caching (improves cache stability across restarts).
# If unset, a random per-process key is used and cache entries will be lost on restart.
# AUTH_CACHE_HMAC_KEY=

# App configuration path inside the container
TACACS_CONFIG=/app/config/tacacs.container.ini

# Optional TACACS server runtime tuning (env overrides)
# TCP backlog for listen()
# TACACS_LISTEN_BACKLOG=128
# Client socket timeout seconds
# TACACS_CLIENT_TIMEOUT=15
# Maximum allowed packet length in bytes
# TACACS_MAX_PACKET_LENGTH=4096
# Enable IPv6 dual-stack listener (true/false)
# TACACS_IPV6_ENABLED=false
# Enable TCP keepalive on client sockets (true/false)
# TACACS_TCP_KEEPALIVE=true
# Keepalive parameters (platform dependent)
# TACACS_TCP_KEEPIDLE=60
# TACACS_TCP_KEEPINTVL=10
# TACACS_TCP_KEEPCNT=5
# Thread pool settings (enable to cap worker threads)
# TACACS_USE_THREAD_POOL=true
# TACACS_THREAD_POOL_MAX=100
